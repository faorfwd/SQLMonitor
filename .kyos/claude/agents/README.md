# Kyos Agents (Managed)

This folder is the framework-managed source for built-in agents.

In a bootstrapped repo:

- `.kyos/claude/agents/` contains these managed agent definitions.
- `.claude/agents/` may contain repo-owned agents (and can include short wrappers pointing to managed ones).

