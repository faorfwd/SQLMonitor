# Local Skills

Put repo-specific skills here. These are repo-owned instructions that complement the managed base under `.kyos/claude/`.

## Available Skills

| Skill | When to use |
|-------|-------------|
| `collection-layer-verify` | Before committing any change to `DDLs/SCH-usp_collect_*.sql`, `DDLs/SCH-Job-*.sql`, or `SQLMonitor/*.ps1` collectors |
| `collection-layer-snippets` | When writing or modifying collection procs, job scripts, or PS collectors — provides canonical `CREATE OR ALTER`, job upsert, and `Write-DbaDbTableData` patterns |
